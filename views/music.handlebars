<body data-content="music">
    <div class="container-fluid" id="big-container">
        <div class="row align-items-start my-5">
            <div class="col-12 col-md-9">
                {{!-- View button --}}
                <div class="dropdown">
                    <div class="row">
                        <div class="col-12 col-md-10">
                            <h5>Remaining albums</h5>
                            <div class="small">
                                * The highest-ranked 50 albums that you haven't interacted with yet.
                            </div>
                        </div>
                        <div class="col col-md-2">
                            {{!-- View button --}}
                            {{> all/view-toggle}}
                        </div>
                    </div>
                </div>
                {{!-- Database flows in here --}}
                {{> music/table-this}}
                <button type="button" class="btn btn-sm" id="refresh">Refresh List</button>
            </div>
        </div>
    </div>
    {{!-- NextUp list - right sidebar --}}
    {{> music/sidebar}}
        
        <p class="pl-3 py-1">Go here to see your<a href="/music/nextup"><strong> full list of
                    NextUp albums</strong></a>.</p>
    </div>
    </div>
    </div>
</body>